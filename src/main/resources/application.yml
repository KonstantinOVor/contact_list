spring:
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    show-sql: true
    hibernate:
      ddl-auto: create-drop
  datasource:
    url: jdbc:mysql://localhost:3305/contacts
    username: mysql
    password: mysql
    driverClassName: com.mysql.cj.jdbc.Driver
    hikari:
      schema: contacts_schema

